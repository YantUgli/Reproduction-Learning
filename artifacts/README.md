# Direktori kerja output Claude Code (M5).
#
# Isi dihasilkan runtime (job Claude Code) — bukan sumber kebenaran kurikulum, jadi
# git-ignored kecuali file ini. Yang di-commit adalah HASIL PROMOSI-nya di `data/`
# setelah lolos gate (skema + test + approve Isyah).
#
# Bentuk: artifacts/<role>/<timestamp>-<rand>/{job.json,prompt.md,<output>}
# Aman dihapus kapan saja: menghapus job = menghapus usulan yang belum di-approve.
